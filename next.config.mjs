/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ui.aceternity.com",
      "kinde.com",
      "nextui.org",
      "images.pexels.com",
    ], // Tambahkan hostname di sini
  },
};

export default nextConfig;
