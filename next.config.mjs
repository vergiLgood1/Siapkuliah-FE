/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ui.aceternity.com",
      "kinde.com"
    ], // Tambahkan hostname di sini
  },
};

export default nextConfig;
